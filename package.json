{
  "name": "gabrielmadeira-site",
  "private": true,
  "description": "Academic website of Gabriel Madeira (Hugo + Hugo Blox + Pagefind)",
  "scripts": {
    "build": "hugo --gc --minify && npx pagefind --site public",
    "dev": "hugo server -D"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "@tailwindcss/typography": "^0.5.20",
    "pagefind": "^1.5",
    "tailwindcss": "^4.1.0"
  }
}
